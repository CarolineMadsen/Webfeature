---
---

<section class="noget">
  <div class="slider">
    <span style="--i:1;"><img src="src/assets/img/img6.png" alt="img6" /></span>
    <span style="--i:2;"><img src="src/assets/img/img7.png" alt="img7" /></span>
    <span style="--i:3;"
      ><img src="src/assets/img/img8.png" ' alt="img8" /></span
    >
    <span style="--i:4;"><img src="src/assets/img/img9.png" alt="img9" /></span>
    <span style="--i:5;"
      ><img src="src/assets/img/img10.png" alt="img10" /></span
    >
    <span style="--i:6;"><img src="src/assets/img/img6.png" alt="img6" /></span>
    <span style="--i:7;"><img src="src/assets/img/img7.png" alt="img7" /></span>
    <span style="--i:8;"><img src="src/assets/img/img8.png" alt="img8" /></span>
  </div>
</section>

<style>
  section {
    display: grid;
    align-items: center;
    justify-content: center;

    height: 100%;
    width: 100%;
  }

  .slider {
    position: relative;
    width: 200px;
    height: 200px;
    transform-style: preserve-3d;
    animation: rotate 50s linear infinite;
  }
  @keyframes rotate {
    0% {
      transform: perspective(1000px) rotateY(0deg);
    }

    100% {
      transform: perspective(1000px) rotateY(360deg);
    }
  }

  .slider span {
    position: absolute;
    top: 0;
    left: 0;
    width: 100%;
    height: 100%;
    transform-origin: center;
    transform-style: preserve-3d;
    transform: rotateY(calc(var(--i) * 45deg)) translateZ(600px);
  }

  .slider span img {
    position: absolute;
    top: 0;
    left: 0;
    width: 100%;
    height: 100%;
    border-radius: 100px;
    object-fit: cover;
    transition: 2s;
  }

  .slider span:hover img {
    transform: translateY(-50px) scale(1.2);
  }

  @media (max-width: 700px) {
    .slider {
      position: relative;
      width: 150px;
      height: 150px;
      transform-style: preserve-3d;
      animation: rotate 50s linear infinite;
    }

    .slider span {
      position: absolute;
      top: 0;
      left: 0;
      width: 100%;
      height: 100%;
      transform-origin: center;
      transform-style: preserve-3d;
      transform: rotateY(calc(var(--i) * 45deg)) translateZ(350px);
    }
  }

  @media (max-width: 500px) {
    .slider {
      position: relative;
      width: 150px;
      height: 150px;
      transform-style: preserve-3d;
      animation: rotate 50s linear infinite;
    }

    .slider span {
      position: absolute;
      top: 0;
      left: 0;
      width: 100%;
      height: 100%;
      transform-origin: center;
      transform-style: preserve-3d;
      transform: rotateY(calc(var(--i) * 45deg)) translateZ(250px);
    }
  }
</style>
